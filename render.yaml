services:
  - type: web
    name: grantmatcher-api
    runtime: docker
    rootDirectory: grantmatcher-api
    envVars:
      - key: DATABASE_URL
        fromSecret: database-url
      - key: SECRET_KEY
        fromSecret: secret-key
    healthCheckPath: /docs